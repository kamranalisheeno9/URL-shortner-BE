<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>URLPro - Sign Up</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600&display=swap" rel="stylesheet" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Poppins", sans-serif;
            background: #fff; /* Removed gradient background */
            color: #fff;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        .container {
            display: flex;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 0; /* Removed border-radius for full-size effect */
            box-shadow: none; /* Removed shadow for a cleaner look */
            width: 100vw; /* Full viewport width */
            height: 100vh; /* Full viewport height */
        }

        .left-section {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 40px;
            text-align: center;
            background: #5e4fa2; /* Solid color to match login UI */
            border-top-left-radius: 0;
            border-bottom-left-radius: 0;
        }

        .left-section h1 {
            font-size: 48px;
            font-weight: 600;
            margin-bottom: 20px;
            color: #fff;
        }

        .left-section p {
            font-size: 16px;
            color: #d1d1d1;
            margin-bottom: 20px;
        }

        .logo {
            font-size: 40px;
            margin-bottom: 20px;
        }

        .right-section {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px;
            background: #fff;
            border-top-right-radius: 0;
            border-bottom-right-radius: 0;
            color: #333;
        }

        .signup-form {
            width: 100%;
            max-width: 300px;
        }

        .signup-form h2 {
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 15px;
            text-align: left;
        }

        .form-group label {
            display: block;
            font-size: 14px;
            font-weight: 500;
            margin-bottom: 5px;
            color: #333;
        }

        .form-group input {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 10px;
            background: #fff;
            color: #333;
            outline: none;
        }

        .btn {
            width: 100%;
            padding: 10px;
            font-size: 16px;
            font-weight: 500;
            background-color: #000; /* Changed to black to match login UI */
            color: #fff;
            border: none;
            border-radius: 20px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #333;
        }

        .message {
            margin-top: 10px;
            padding: 10px;
            border-radius: 10px;
            display: <%= typeof message !== 'undefined' ? 'block' : 'none' %>;
        }

        .error {
            background-color: #ffcccc;
            color: #cc0000;
        }

        .success {
            background-color: #ccffcc;
            color: #006600;
        }

        .login-link {
            margin-top: 15px;
            font-size: 14px;
            color: #666;
        }

        .login-link a {
            color: #3281c4;
            text-decoration: none;
        }

        .login-link a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="left-section">
            <div class="logo">⭐</div>
            <h1>Welcome to URL Pro</h1>
            <p>
                Make Every Link Count – Shorten and Optimize with URL Pro!
            </p>
            <p>© 2025 URLPro. All rights reserved.</p>
          </div>
        <div class="right-section">
            <div class="signup-form">
                <h2>Sign Up</h2>
                <% if (typeof message !== 'undefined') { %>
                    <!-- <div class="message <%= message.type %>"><%= message.text %></div> -->
                <% } %>
                <form action="/api/signup" method="POST">
                    <div class="form-group">
                        <label for="firstName">First Name</label>
                        <input type="text" id="firstName" name="firstName" required />
                    </div>
                    <div class="form-group">
                        <label for="lastName">Last Name</label>
                        <input type="text" id="lastName" name="lastName" required />
                    </div>
                    <div class="form-group">
                        <label for="email">Email</label>
                        <input type="email" id="email" name="email" required />
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" id="password" name="password" required />
                    </div>
                    <button type="submit" class="btn">Sign Up</button>
                </form>
                <div class="login-link">
                    Already have an account? <a href="/">Log In</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>